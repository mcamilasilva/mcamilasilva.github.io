import { EducationAdditionalInfo } from './education-additional-info';

describe('EducationAdditionalInfo', () => {
  it('should create an instance', () => {
    expect(new EducationAdditionalInfo()).toBeTruthy();
  });
});
