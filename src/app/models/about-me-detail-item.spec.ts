import { AboutMeDetailItem } from './about-me-detail-item';

describe('AboutMeDetailItem', () => {
  it('should create an instance', () => {
    expect(new AboutMeDetailItem()).toBeTruthy();
  });
});
